class BasicOperator{
    public static void main(String[] args)
    {   
        int x = 5;
        int y = 2;
        double z = 57;
        double u = z / y;
        double d = Math.pow(x,y);
        double p = x / (double)y;
        System.out.println(p);
        
    }
}